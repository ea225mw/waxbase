/**
 * ONLY FOR TRYING OUT TESTING WITH JEST. CAN BE REMOVED LATER ON.
 * Multiplies x by y.
 *
 * @param {number} x - The first parameter.
 * @param {number} y - The second parameter.
 * @returns {number} - The calculated result.
 */
export function multiply (x, y) {
  return x * y
}
